Run the `setup-parallax` skill from `skills/setup-parallax/SKILL.md`.

1. List AVIF files in `docs/assets/hero/` and show the user what was found.
2. Ask for hero headline, subtext, and button labels if not provided.
3. Write `docs/overrides/home.html`, `docs/assets/stylesheets/home.css`, and update `mkdocs.yml`.
4. Run `mkdocs build` and report the result.
5. Show a summary table: layer filename → depth → image-position.