Run the `tune-layers` skill from `skills/tune-layers/SKILL.md`.

1. Read `docs/overrides/home.html` and print a table of current layers with their depth and image-position values.
2. Ask the user which layer looks wrong and what the visual problem is.
3. Propose new values with a one-sentence explanation of the expected visual change.
4. Apply the changes and run `mkdocs build`.

Arguments: $ARGUMENTS