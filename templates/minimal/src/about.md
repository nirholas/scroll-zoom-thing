# About

Replace this content with a paragraph or two about the project.

This file exists so the navigation has at least one non-home entry. Add
more pages by creating markdown files under `src/` and listing them in
`mkdocs.yml` under `nav:`.
