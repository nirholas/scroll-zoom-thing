# Getting started with scroll-zoom-thing

The repository is a working production site. The docs you see when you build it are the PAI documentation, included verbatim as a real-world example of the parallax pattern in use. Replace the content in `src/` with your own and you have a cinematic, depth-driven docs site for your own project.

## Install

```bash
pip install -r requirements.txt
```

## Verify the install

Clone the repository and run its checks to confirm everything works on your machine:

```bash
git clone https://github.com/nirholas/scroll-zoom-thing.git
cd scroll-zoom-thing
```



## Next steps

- [Examples](./examples.md) shows runnable snippets.
- The [README](https://github.com/nirholas/scroll-zoom-thing#readme) is the complete reference.
- Found a problem? [Open an issue](https://github.com/nirholas/scroll-zoom-thing/issues).
